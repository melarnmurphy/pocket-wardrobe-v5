# Fashion Pipeline — self-hosted local development dependencies.
# Run with:
#   uvicorn pipeline.server:app --host 0.0.0.0 --port 8000

# ML — PyTorch with MPS support is included in standard torch>=1.12 on Apple Silicon
torch>=2.0
torchvision
transformers>=4.40,<5.0
Pillow>=10.0

# Web layer
fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0

# Vector/search utilities used by the model stack
faiss-cpu
ftfy
open_clip_torch
